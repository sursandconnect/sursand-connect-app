SURSAND CONNECT v10.5 — HOME PRESERVED / WARD 19 ONLY

This package is built from the user's uploaded Sursand_Connect_App_v10_5(1).zip.

IMPORTANT:
- index.html (Home) is preserved byte-for-byte from the uploaded v10.5 file.
- No Home design, Home cards, Home scripts, navigation, popup system, account system,
  news/update section, or other Home content was edited.

Changes only:
1. Representatives now accepts Ward 19 instead of stopping at Ward 18.
2. City Connect has Ward 19 / S19 directly after Ward 18 / S18, cloned from the
   existing Ward 18 card so the existing design is preserved.
3. Explicit 18-ward text/ranges on those relevant pages are changed to 19.
4. Service-worker cache name is bumped so the corrected pages can refresh.

No dummy councillor name, phone number, or WhatsApp URL was invented.
Existing app/backend data should supply those values.
